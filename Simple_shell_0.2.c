#include "main.h"

/**
 * main - handle  arguments
 *
 * Return: Always 0 in success
 */
int main(int argc, char *argv[])
{
	int argc;
	int *argv;
	int i = 0;

	if (i != 0, i < argc, i++)
		write(STDOUT_FILENO, char *argv, argv[]);
	if (i = 0, i = argc)
		write(STDOUT_FILENO, char *argv, argv[0]);
	return (0);
}
